package JSci.maths;

/**
* This interface defines an N dimensional map.
* @see Mapping
* @see ComplexMapping
* @version 1.1
* @author Mark Hale
*/
public interface MappingND {
        /**
        * A user-defined map.
        */
        double[] map(double x[]);
}

